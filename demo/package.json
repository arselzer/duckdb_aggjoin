{
  "name": "aggjoin-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "In-browser demo for the DuckDB AggJoin extension — runs entirely client-side via DuckDB-WASM.",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview --port 4180 --strictPort"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "1.32.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
